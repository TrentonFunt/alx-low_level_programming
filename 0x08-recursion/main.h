#ifndef MAIN_H
#define MAIN_H

/* Prototype for the factorial function */
int factorial(int n);

/* Prototype for the _strlen_recursion function */
int _strlen_recursion(char *s);

/* Prototype for the _print_rev_recursion function */
void _print_rev_recursion(char *s);

/* Prototype for the _putchar function */
int _putchar(char c);

/* Prototype for the _puts_recursion function */
void _puts_recursion(char *s);

#endif /* MAIN_H */
