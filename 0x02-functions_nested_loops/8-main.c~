#include "main.h"

/**
 * main - Entry point
 *
 * Description: This is the main function that calls `jack_bauer` to print
 * every minute of the day of Jack Bauer.
 *
 * Return: 0 (Success)
 */
int main(void)
{
    jack_bauer();
    return (0);
}
